// teaserAgent.ts — NanoBanana API로 티저 이미지 1장 생성
// input:  { city: string, moodName: string, faceUrl?: string, env: Bindings }
// output: { imageUrl: string, jobId: string }
// 비용 규칙: 1장만 실제 생성. 나머지 3장은 프론트에서 CSS blur+tint overlay 처리.

export {}
